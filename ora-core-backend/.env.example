NODE_ENV=development
PORT=3333
CORS_ORIGIN=http://localhost:5173

# GitHub canon source. V1 uses local mock seeds and keeps this URL as metadata.
GITHUB_ORG_URL=https://github.com/TwinsProductionAI
GITHUB_TOKEN=

# V1 does not call a native generative AI provider.
# Future providers must stay server-side and require explicit product/safety wiring.
LLM_PROVIDER=stub
LLM_API_KEY=
LLM_MODEL=